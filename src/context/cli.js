const base = require('./default');
const clone = require('../utils').clone;

var context = clone(base);

// TODO: Add support for doAsk

// TODO: Add support for key events

module.exports = context;
